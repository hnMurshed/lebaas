import React from 'react';
import { useParams } from 'react-router-dom';

const BlogDetails = () => {
    return (
        <div className='container p-8'>
            <h3 className='text-2xl'>What is Context API and what is the purpose of it?</h3>
        </div>
    );
};

export default BlogDetails;